name: create-map
on: push

jobs:
  run_tests:
    runs-on: ubuntu-latest
    steps:
    
    - name: Run script file
      run: |
        chmod +x "${GITHUB_WORKSPACE}/testing.sh"
    - run:  bash ${GITHUB_WORKSPACE}/testing.sh
      
